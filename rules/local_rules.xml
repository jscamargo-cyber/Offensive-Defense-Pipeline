<group name="local,syslog,sshd,">
  <!-- Regla para capturar ataques X-FILTR desde syslog -->
  <rule id="100002" level="12">
    <if_sid>1002</if_sid> <!-- Captura logs genéricos de syslog -->
    <match>X-FILTR attack detected</match>
    <description>X-FILTR: ALERTA CRITICA - EXFILTRACION DE DATOS DETECTADA (PUERTO 4444)</description>
    <mitre>
      <id>T1041</id>
    </mitre>
  </rule>
</group>
